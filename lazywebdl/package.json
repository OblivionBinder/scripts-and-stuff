{
  "name": "lazywebdl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild src/background/background.ts --bundle --sourcemap --format=esm --outfile=dist/background.bundled.js --platform=browser",
    "lint": "web-ext lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "esbuild": "0.28.1",
    "typescript": "^6.0.3"
  }
}
